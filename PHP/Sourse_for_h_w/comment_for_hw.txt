1. домашнее задание Слиент серверная система, основы html
Нужно работать над красивыми отступами, но вцелом все класно!

2. домашнее задание HTML Forms CSS Bootstrap (со втоорого раза здана)
Вцелом хорошо, но есть несколько моментов:
1. В отступах намешал пробелы и табуляцию, я говорил так не делать
2. В футере проблемы с шириной, если откроеш страницу, увидишь что он уехал вправо
3. <label for="valuteId"></label> если лейбл пустой, нет смысла писать этот тег
4. На страничке контактов оступ на один больше чем нужно в контейнере
[UPD]
1. </fieldset><!-- End group element form -->
Не вижу открывающего тега, его нет
2. <a href="mailto:#">support@learnphp.com</a>
почему тут решетку оставил, нужно было в href вставить адрес и тогда бы работала отправка емейла

3. PHP Basic
Вцелом все отлично, но на странице с контактами нужно было побольше переменных вынести

4. Неправильно определены константы,      СТАРАЙСЯ ИСПОЛЬЗОВАТЬ ОДИНАРНЫЕ
КАВЫЧКИ     в глобальной области лучше использовать define 
5. Условные операторы, Подключение файлов     
	Вцелом все красиво и отлично. Вот пара замечаний:
	    1. опечатка
	    2. не хватает отступа 
6. Создать форму регистрации
     1. Я пробовал открыть клнтроллнр просто без POST
пареметров мне ничего не выбело это плохо.     2. Нельзя отображать данные
пользователя от регестрации в строке поиска URL     3. Нужно постоянно
проверять пришли ли данные от пользовалетя     4. слишком много файлов, файл
Login.php от куда я знаю что  форма лежит там? 
7. Functions      1.
controller.php     нужно было вынести все функции в отдельный файл, а логику
оставить в этом. У тебя код разнесен,      но разнесение кода по отдельным
файлам должно упрощать читаемость, а у тебя не упрощает. Строчки      между
функциями выглядят странно, функции должны лежать где-то отдельно от их
использования     2. isUah(...  функции с таким именем должны возвращать true
или false, иначе непонятно что она делает.     3. function
changeInUah($tovars, $course)     впринципе функция написана правильно, но она
несильно универсальная, ее нельзя переиспользовать,     она работает только со
списком определеннных товаров, и если что-то поменяется ее всегда нужно
менять. Нужно в функции выносить более простые вещи, просто конвертацию валюты
а не всего списка товаров.     4. а вот с дискаунтом сделал получше. Функция
считает скидку. Правда она опять же работает с товаром,      это не очень
хорошо, но преемлемо. По хорошему эти функции про товар ничего не должны
знать, они должны      принимать просто цифры и тип - тип валюты или тип
скидки, и преобразовывать, а не работать с массивом       конкретного формата.
