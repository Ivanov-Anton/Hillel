Создать приложение для управления учебным процессом
Часть 1

База данных состоит из одной таблицы members:
id - int - первичный ключ, создаем так же как мы это делали на уроке
fullName - varchar(255)
phone - varchar(255)
email - varchar(255)
role - varchar(255)
averangeMark - float
subject - varchar(255)
workingDay - varchar(255)

Создаем файлы:

migrate_db.php - создаем таблицу и заполняем ее тестовыми данными


Часть 2

На основании имеющейся базы данных.

Создать небольшое приложение для управления учебным процессом.

Имеется 3 типа записей: Студент, преподаватель и администратор.

Каждая запись имеет свойства: ФИО, телефон, email и роль(студент, преподаватель и администратор).

Создаем базовый класс Person, с общими свойствами и методами, наследуемся от него и в дочерних классах добавляем/меняем только необходиые свойства.

Студенты дополнительно имеют свойство с средним балом успеваемости оценок и посещаемостью в процентах.

Дополнительное свойство преподавателей - название преподаваемого предмета.

Дополнительное свойство администраторов - день в который он работает:например monday

У всех записей есть метод - getInfo - который возвращает все данные одной строкой.